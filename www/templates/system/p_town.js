$script.ready('app', function(){
	//$script(['jquery.form'], 'defs');

	//$script.ready('defs', function() {
		IWars.page.p_town = {
			init: function(){
			}
		}

		//IWars.page.defs.init();
	//});
})